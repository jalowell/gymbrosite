import { Component } from "react";
import "../styling/Workout.css";

export default class Workout extends Component {
    render() {
        return (
            <div>
                <h2>Workout</h2>
                <p>Welcome to the Workout page.</p>
            </div>
        );
    }
}